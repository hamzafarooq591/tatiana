{% comment %}
  TatianaGlen Homepage — Static Clone
  Source: https://tatianaglen.com/
  Sections (top→bottom, header & footer excluded):
    1. Hero Slider
    2. Category Blocks
    3. The Collection (Product Grid)
    4. Process Section
    5. Meet The Artist
    6. Craft & Commitment (Trust Badges)
    7. Instagram / Social
{% endcomment %}

{% comment %} Inject Google Fonts into <head> via Shopify's content_for_header hook {% endcomment %}
{%- capture tg_font_links -%}
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;1,300;1,400&family=Montserrat:wght@300;400;500;600;700&family=Open+Sans:wght@300;400;500;600&family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
{%- endcapture -%}
{{ tg_font_links }}

<style>
  /* ============================================================
     HOMEPAGE CSS VARIABLES
     ============================================================ */
  .tg-homepage {
    --tg-primary:       #CD8F5C;
    --tg-primary-hover: #bc753a;
    --tg-secondary:     #EAF1F5;
    --tg-text:          #666666;
    --tg-accent:        #111111;
    --tg-border:        #EEEEEE;
    --tg-light:         #767676;
    --tg-cream:         #f7f1ed;
    --tg-dark-brown:    #5d2b09;
    --tg-serif:         'Cormorant Garamond', Georgia, serif;
    --tg-sans:          'Open Sans', Arial, sans-serif;
    font-family: var(--tg-sans);
  }

  /* Global heading override — capitalize, never uppercase */
  .tg-homepage h1,
  .tg-homepage h2,
  .tg-homepage h3,
  .tg-homepage h4,
  .tg-homepage h5,
  .tg-homepage h6,

  /* Buttons and CTAs stay uppercase */
  .tg-btn-primary,
  .tg-btn-outline,
  .tg-link-underline,
  .tg-hero__cta,
  .tg-category__link,
  .tg-product-card__btn,
  .tg-product-card__icon-btn,
  .tg-artist__link,
  .tg-instagram__follow {
    text-transform: uppercase !important;
  }

  /* ============================================================
     1. HERO SLIDER
     ============================================================ */
  .tg-hero {
    position: relative;
    width: 100%;
    height: 800px;
    overflow: hidden;
    background: #111;
  }

  .tg-hero__slide {
    position: absolute;
    inset: 0;
    opacity: 0;
    transition: opacity 1.5s ease-in-out;
    background-size: cover;
    background-position: center;
    display: flex;
    align-items: center;
    justify-content: flex-start;
  }

  .tg-hero__slide.is-active {
    opacity: 1;
  }

  .tg-hero__overlay {
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.42);
  }

  .tg-hero__content {
    position: relative;
    z-index: 2;
    padding: 0 80px;
    max-width: 740px;
    padding-top: 20%;
  }

  .tg-hero__heading {
    font-family: var(--tg-serif);
    font-size: 72px;
    font-weight: 400;
    line-height: 1;
    color: #ffffff;
    margin: 0 0 10px;
    letter-spacing: 0.02em;
    opacity: 0;
    transform: translateY(32px);
    transition: opacity 0.9s ease 0.3s, transform 0.9s ease 0.3s;
  }

  .tg-hero__slide.is-active .tg-hero__heading {
    opacity: 1;
    transform: translateY(0);
  }

  .tg-hero__subheading {
    font-family: "Open Sans", sans-serif;
    font-size: 23px;
    font-weight: 400;
    font-style: italic;
    color: rgb(255, 255, 255);
    margin: 0 0 20px;
    line-height: 1.4em;
    opacity: 0;
    transform: translateY(22px);
    transition: opacity 0.9s ease 0.52s, transform 0.9s ease 0.52s;
  }

  .tg-hero__slide.is-active .tg-hero__subheading {
    opacity: 1;
    transform: translateY(0);
  }

  .tg-hero__cta {
    display: inline-block;
    font-family: var(--tg-sans);
    font-size: 16px;
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: #111111;
    background: #ffffff;
    padding: 16px 38px;
    text-decoration: none;
    opacity: 0;
    transform: translateY(20px);
    transition: opacity 0.9s ease 0.72s, transform 0.9s ease 0.72s, background 0.35s ease, color 0.35s ease;
  }

  .tg-hero__slide.is-active .tg-hero__cta {
    opacity: 1;
    transform: translateY(0);
  }

  .tg-hero__cta:hover {
    background: var(--tg-primary);
    color: #ffffff;
  }

  /* Slide dots */
  .tg-hero__dots {
    position: absolute;
    right: 28px;
    top: 50%;
    transform: translateY(-50%);
    display: flex;
    flex-direction: column;
    gap: 12px;
    z-index: 10;
  }

  .tg-hero__dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.4);
    border: none;
    cursor: pointer;
    padding: 0;
    transition: background 0.3s ease, transform 0.3s ease;
  }

  .tg-hero__dot.is-active {
    background: #ffffff;
    transform: scale(1.35);
  }

  /* Hero arrow nav */
  .tg-hero__arrow {
    display: none !important;
  }
  .tg-hero__arrow--hide {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    z-index: 10;
    background: rgba(255,255,255,0.12);
    border: 1px solid rgba(255,255,255,0.3);
    color: #fff;
    width: 48px;
    height: 48px;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: background 0.3s ease;
    backdrop-filter: blur(4px);
  }

  .tg-hero__arrow:hover {
    background: rgba(255,255,255,0.25);
  }

  .tg-hero__arrow--prev { left: 24px; }
  .tg-hero__arrow--next { right: 24px; }

  .tg-hero__arrow svg {
    width: 18px;
    height: 18px;
    stroke: currentColor;
    fill: none;
    stroke-width: 2;
    stroke-linecap: round;
    stroke-linejoin: round;
  }

  /* ============================================================
     2. CATEGORY BLOCKS
     ============================================================ */
  .tg-categories-wrapper {
    background: #ffffff;
    padding: 70px 45px;
  }

  .tg-categories {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 50px;
  }

  .tg-category {
    position: relative;
    height: 500px;
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
    text-decoration: none;
  }

  .tg-category__bg {
    position: absolute;
    inset: 0;
    background-size: cover;
    background-position: center;
    transition: transform 1.5s cubic-bezier(0.25, 0.46, 0.45, 0.94),
                filter 0.6s ease;
    filter: blur(0px);
  }

  .tg-category:hover .tg-category__bg {
    transform: scale(1.05);
    filter: blur(3px);
  }

  .tg-category__overlay {
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.25);
    transition: background 0.4s ease;
  }

  .tg-category:hover .tg-category__overlay {
    background: rgba(0, 0, 0, 0.38);
  }

  .tg-category__content {
    position: relative;
    z-index: 2;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: 32px 10px;
    top: 30%;
  }

  .tg-category__title {
    font-family: var(--tg-serif);
    font-size: 47px;
    color: #ffffff;
    margin: 0 0 10px;
  }

  .tg-category__link {
    display: inline-block;
    font-family: var(--tg-sans);
    font-size: 16px;
    font-weight: 600;
    letter-spacing: 0.02em;
    text-transform: uppercase;
    color: #111111;
    background: #ffffff;
    border: 1px solid #ffffff;
    padding: 16px 42px;
    transition: background 0.3s ease, color 0.3s ease, border-color 0.3s ease;
  }

  .tg-category:hover .tg-category__link {
    background: transparent;
    color: #ffffff;
    border-color: #ffffff;
  }

  /* ============================================================
     3. THE COLLECTION
     ============================================================ */
  .tg-collection {
    padding: 90px 80px;
    background: #ffffff;
  }

  .tg-section-header {
    text-align: center;
    margin-bottom: 56px;
  }

  .tg-section-header__title {
    font-family: var(--tg-serif);
    font-size: 48px;
    font-weight: 400;
    color: var(--tg-accent);
    margin: 0 0 22px;
    letter-spacing: 0.04em;
  }

  .tg-section-header__desc {
    font-family: var(--tg-sans);
    font-size: 15px;
    color: var(--tg-text);
    max-width: 640px;
    margin: 0 auto;
    line-height: 1.85;
  }

  /* Product Slider */
  .tg-product-slider-wrap {
    position: relative;
    margin-bottom: 56px;
    overflow: hidden; /* clip here, NOT on the track */
  }

  .tg-product-grid {
    display: flex;
    gap: 32px;
    will-change: transform;
    /* transition set via JS */
  }

  .tg-product-card {
    position: relative;
    cursor: pointer;
    flex: 0 0 calc((100% - 64px) / 3);
    min-width: 0;
  }

  /* Full-card clickable link */
  .tg-product-card__link {
    display: block;
    text-decoration: none;
    color: inherit;
  }

  /* Action buttons overlaid on image */
  .tg-product-card__actions {
    position: absolute;
    bottom: 16px;
    left: 0;
    right: 0;
    z-index: 2;
    display: flex;
    justify-content: center;
    gap: 10px;
    opacity: 0;
    transform: translateY(10px);
    transition: opacity 0.35s ease, transform 0.35s ease;
  }

  .tg-product-card:hover .tg-product-card__actions {
    opacity: 1;
    transform: translateY(0);
  }

  .tg-product-card__img-wrap {
    position: relative;
    overflow: hidden;
    aspect-ratio: 450 / 532;
    background: #f4ede6;
    margin-bottom: 18px;
  }

  .tg-product-card__img {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transition: transform 1.5s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  }

  .tg-product-card:hover .tg-product-card__img {
    transform: scale(1.04);
  }

  /* Round icon actions */
  .tg-product-card__actions {
    position: absolute;
    bottom: 16px;
    left: 0;
    right: 0;
    display: flex;
    justify-content: center;
    gap: 10px;
    opacity: 0;
    transform: translateY(10px);
    transition: opacity 0.35s ease, transform 0.35s ease;
  }

  .tg-product-card:hover .tg-product-card__actions {
    opacity: 1;
    transform: translateY(0);
  }

  .tg-product-card__icon-btn {
    width: 44px;
    height: 44px;
    border-radius: 50%;
    background: #111111;
    color: #ffffff;
    border: none;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    text-decoration: none;
    transition: background 0.3s ease, transform 0.3s ease;
    flex-shrink: 0;
  }

  .tg-product-card__icon-btn:hover {
    background: var(--tg-primary);
    transform: scale(1.1);
  }

  .tg-product-card__icon-btn--disabled {
    opacity: 0.4;
    cursor: not-allowed;
  }

  .tg-product-card__sold-out {
    color: var(--tg-light);
    font-size: 12px;
  }

  .tg-placeholder {
    background: #f4ede6;
  }

  .tg-product-card__icon-btn svg {
    width: 18px;
    height: 18px;
    stroke: #ffffff;
    fill: none;
    stroke-width: 1.8;
    stroke-linecap: round;
    stroke-linejoin: round;
  }

  /* Slider dots */
  .tg-product-dots {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 6px;
    margin-top: 32px;
  }

  .tg-product-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: #d0d0d0;
    border: none;
    cursor: pointer;
    padding: 0;
    flex-shrink: 0;
    transition: width 0.35s ease, border-radius 0.35s ease, background 0.35s ease;
  }

  .tg-product-dot.is-active {
    width: 28px;
    border-radius: 4px;
    background: var(--tg-primary);
  }

  .tg-product-card__info {
    padding: 0 2px;
  }

  .tg-product-card__title {
    font-family: 'Montserrat', sans-serif;
    font-size: 20px;
    font-weight: 400;
    color: var(--tg-accent);
    margin: 0 0 8px;
    letter-spacing: 0.02em;
    line-height: 1.35;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .tg-product-card__price {
    font-family: 'Montserrat', sans-serif;
    font-size: 16px;
    font-weight: 600;
    color: #111111;
    margin: 0;
  }

  .tg-collection-footer {
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 18px;
  }

  /* ============================================================
     SHARED BUTTON STYLES
     ============================================================ */
  .tg-btn-primary {
    display: inline-block;
    font-family: var(--tg-sans);
    font-size: 11px;
    font-weight: 700;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: #ffffff;
    background: var(--tg-accent);
    padding: 17px 44px;
    text-decoration: none;
    transition: background 0.4s ease, transform 0.4s ease;
  }

  .tg-btn-primary:hover {
    background: var(--tg-primary);
    transform: translateY(-1px);
  }

  .tg-link-underline {
    padding: 16px 41px;
    font-size: 18px;
    font-weight: 400;
    line-height: 24px;
    color: #FFFFFF;
    border-style: solid;
    border-width: 2px;
    border-color: var(--tg-primary);
    border-radius: 0;
    background-color: #111111;
    text-decoration: none;
    display: inline-block;
    transition: background 0.3s ease, color 0.3s ease;
  }

  .tg-link-underline:hover {
    color: var(--tg-primary);
    border-color: var(--tg-primary);
  }

  /* ============================================================
     3b. QUOTE BANNER + SAGE BAND
     ============================================================ */
  .tg-quote-banner {
    position: relative;
    width: 100%;
    min-height: 320px;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    background-image: url('https://tatianaglen.com/wp-content/uploads/2026/02/2-1.png');
    background-size: cover;
    background-position: center;
    padding: 70px 80px;
    text-align: center;
  }

  .tg-quote-banner__overlay {
    position: absolute;
    inset: 0;
    background: rgba(20, 14, 8, 0.38);
  }

  .tg-quote-banner__text {
    position: relative;
    z-index: 2;
    max-width: 860px;
    font-family: var(--tg-serif);
    font-size: 42px;
    font-weight: 400;
    line-height: 1.25;
    color: #ffffff;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    margin: 0;
  }

  .tg-quote-banner__text em {
    font-style: italic;
    font-weight: 400;
  }

  .tg-sage-band {
    background: #b8c4af;
    padding: 48px 80px;
    text-align: center;
  }

  .tg-sage-band__text {
    font-family: var(--tg-serif);
    font-size: 22px;
    font-weight: 400;
    font-style: italic;
    color: #2a2a2a;
    margin: 0;
    letter-spacing: 0.01em;
  }

  @media (max-width: 989px) {
    .tg-quote-banner       { padding: 60px 40px; }
    .tg-quote-banner__text { font-size: 32px; }
    .tg-sage-band          { padding: 40px 40px; }
    .tg-sage-band__text    { font-size: 19px; }
  }

  @media (max-width: 749px) {
    .tg-quote-banner       { padding: 50px 24px; }
    .tg-quote-banner__text { font-size: 24px; letter-spacing: 0.04em; }
    .tg-sage-band          { padding: 36px 24px; }
    .tg-sage-band__text    { font-size: 17px; }
  }

  /* ============================================================
     4. PROCESS SECTION
     ============================================================ */
  .tg-process {
    position: relative;
    min-height: 620px;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 110px 60px;
    text-align: center;
    overflow: hidden;
  }

  .tg-process__bg {
    position: absolute;
    inset: 0;
    background-image: url('https://tatianaglen.com/wp-content/uploads/2026/03/Resina-home-scaled.png');
    background-size: cover;
    background-position: center;
    will-change: transform;
  }

  .tg-process__overlay {
    position: absolute;
    inset: 0;
    background: rgba(15, 8, 3, 0.58);
  }

  .tg-process__content {
    position: relative;
    z-index: 2;
    max-width: 800px;
  }

  .tg-process__quote {
    font-family: var(--tg-serif);
    font-size: 34px;
    font-weight: 300;
    font-style: italic;
    color: rgba(255, 255, 255, 0.92);
    line-height: 1.55;
    margin: 0 0 44px;
    letter-spacing: 0.01em;
  }

  .tg-process__divider {
    width: 56px;
    height: 1px;
    background: rgba(205, 143, 92, 0.7);
    margin: 0 auto 44px;
  }

  .tg-process__subheading {
    font-family: var(--tg-sans);
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 0.28em;
    text-transform: uppercase;
    color: var(--tg-primary);
    margin: 0 0 28px;
  }

  .tg-process__cta-heading {
    font-family: var(--tg-serif);
    font-size: 52px;
    font-weight: 400;
    color: #ffffff;
    margin: 0 0 30px;
    line-height: 1.18;
    letter-spacing: 0.02em;
  }

  .tg-process__desc {
    font-family: var(--tg-sans);
    font-size: 15px;
    color: rgba(255, 255, 255, 0.78);
    line-height: 1.9;
    margin: 0 0 44px;
  }

  .tg-btn-outline {
    display: inline-block;
    font-family: var(--tg-sans);
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: #ffffff;
    border: 1px solid rgba(255, 255, 255, 0.65);
    padding: 16px 38px;
    text-decoration: none;
    transition: background 0.35s ease, border-color 0.35s ease;
  }

  .tg-btn-outline:hover {
    background: rgba(255, 255, 255, 0.1);
    border-color: #ffffff;
  }

  /* ============================================================
     5. MEET THE ARTIST
     ============================================================ */
  .tg-artist {
    display: grid;
    grid-template-columns: 1fr 1fr;
    min-height: 580px;
  }

  .tg-artist__img-side {
    position: relative;
    overflow: hidden;
    min-height: 460px;
  }

  .tg-artist__img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transition: transform 1.5s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  }

  .tg-artist__img-side:hover .tg-artist__img {
    transform: scale(1.04);
  }

  .tg-artist__content {
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 80px 80px;
    background: var(--tg-cream);
  }

  .tg-artist__label {
    font-family: var(--tg-sans);
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0.32em;
    text-transform: uppercase;
    color: var(--tg-primary);
    margin-bottom: 22px;
    display: block;
  }

  .tg-artist__heading {
    font-family: var(--tg-serif);
    font-size: 40px;
    font-weight: 400;
    color: var(--tg-accent);
    margin: 0 0 28px;
    line-height: 1.2;
    letter-spacing: 0.02em;
  }

  .tg-artist__bio {
    font-family: var(--tg-sans);
    font-size: 14px;
    color: var(--tg-text);
    line-height: 1.95;
    margin: 0 0 18px;
  }

  .tg-artist__bio:last-of-type {
    margin-bottom: 36px;
  }

  .tg-artist__link {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    font-family: var(--tg-sans);
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: var(--tg-accent);
    text-decoration: none;
    border-bottom: 1px solid var(--tg-accent);
    padding-bottom: 5px;
    transition: color 0.3s ease, border-color 0.3s ease;
    width: fit-content;
  }

  .tg-artist__link:hover {
    color: var(--tg-primary);
    border-color: var(--tg-primary);
  }

  /* ============================================================
     6. CRAFT & COMMITMENT
     ============================================================ */
  .tg-trust {
    padding: 90px 60px;
    background: #ffffff;
    text-align: center;
    width: 100%;
    box-sizing: border-box;
  }

  .tg-trust__heading {
    font-family: var(--tg-serif);
    font-size: 38px;
    font-weight: 400;
    color: var(--tg-accent);
    margin: 0 0 64px;
    letter-spacing: 0.04em;
  }

  .tg-trust__grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 48px;
    width: 100%;
  }

  .tg-trust__item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 22px;
  }

  .tg-trust__icon {
    width: 68px;
    height: 68px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .tg-trust__icon svg {
    width: 52px;
    height: 52px;
    stroke: var(--tg-dark-brown);
    fill: none;
    stroke-width: 1.2;
    stroke-linecap: round;
    stroke-linejoin: round;
  }

  .tg-trust__item-title {
    font-family: var(--tg-serif);
    font-size: 21px;
    font-weight: 400;
    color: var(--tg-accent);
    margin: 0 0 10px;
    letter-spacing: 0.02em;
    line-height: 1.3;
  }

  .tg-trust__item-desc {
    font-family: var(--tg-sans);
    font-size: 14px;
    color: var(--tg-text);
    line-height: 1.75;
    margin: 0;
    max-width: 260px;
  }

  /* ============================================================
     7. INSTAGRAM / SOCIAL
     ============================================================ */
  .tg-instagram {
    padding: 90px 80px;
    background: var(--tg-cream);
    background-image: url('https://tatianaglen.com/wp-content/uploads/2026/02/3.png');
    background-size: cover;
    background-position: center;
    text-align: center;
    position: relative;
  }

  .tg-instagram__heading {
    font-family: var(--tg-serif);
    font-size: 44px;
    font-weight: 400;
    color: #ffffff;
    margin: 0 0 14px;
    letter-spacing: 0.02em;
  }

  .tg-instagram__subtext {
    font-family: var(--tg-sans);
    font-size: 15px;
    color: rgba(255, 255, 255, 0.85);
    margin: 0 0 36px;
  }

  .tg-instagram__social {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 14px;
    flex-wrap: wrap;
  }

  .tg-instagram__follow {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    font-family: var(--tg-sans);
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: #ffffff;
    background: transparent;
    border-bottom: 1px solid #ffffff;
    padding: 4px 0;
    text-decoration: none;
    transition: color 0.35s ease, border-color 0.35s ease;
  }

  .tg-instagram__follow:hover {
    color: var(--tg-primary);
    border-color: var(--tg-primary);
  }

  .tg-instagram__follow svg {
    width: 16px;
    height: 16px;
    fill: currentColor;
    flex-shrink: 0;
  }

  .tg-instagram__social-link {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 46px;
    height: 46px;
    border: 1px solid var(--tg-border);
    color: var(--tg-text);
    text-decoration: none;
    transition: border-color 0.3s ease, color 0.3s ease;
  }

  .tg-instagram__social-link:hover {
    border-color: var(--tg-primary);
    color: var(--tg-primary);
  }

  .tg-instagram__social-link svg {
    width: 18px;
    height: 18px;
    fill: currentColor;
  }

  /* ============================================================
     SCROLL-IN ANIMATIONS
     ============================================================ */
  .tg-animate {
    opacity: 0;
    transform: translateY(36px);
    transition: opacity 0.85s ease, transform 0.85s ease;
  }

  .tg-animate.is-visible {
    opacity: 1;
    transform: translateY(0);
  }

  .tg-animate--delay-1 { transition-delay: 0.10s; }
  .tg-animate--delay-2 { transition-delay: 0.20s; }
  .tg-animate--delay-3 { transition-delay: 0.32s; }
  .tg-animate--delay-4 { transition-delay: 0.44s; }

  /* ============================================================
     RESPONSIVE — TABLET (≤ 989px)
     ============================================================ */
  @media (max-width: 989px) {
    .tg-hero            { height: 560px; }
    .tg-hero__content   { padding: 0 40px; max-width: 560px; }
    .tg-hero__heading   { font-size: 52px; }

    .tg-categories-wrapper { padding: 20px 14px; }
    .tg-categories      { grid-template-columns: 1fr; gap: 20px; }
    .tg-category        { height: 360px; }

    .tg-collection      { padding: 70px 40px; }
    .tg-product-card    { flex: 0 0 calc((100% - 20px) / 2); }

    .tg-process         { padding: 90px 50px; }
    .tg-process__cta-heading { font-size: 40px; }
    .tg-process__quote  { font-size: 26px; }

    .tg-artist          { grid-template-columns: 1fr; }
    .tg-artist__img-side{ height: 400px; }
    .tg-artist__content { padding: 60px 50px; }

    .tg-trust           { padding: 70px 40px; }
    .tg-trust__grid     { grid-template-columns: 1fr; gap: 48px; }

    .tg-instagram       { padding: 70px 40px; }
  }

  /* ============================================================
     RESPONSIVE — MOBILE (≤ 749px)
     ============================================================ */
  @media (max-width: 749px) {
    .tg-hero            { height: 650px; }
    .tg-hero__content   { padding: 0 12px; top: 25%; }
    .tg-hero__heading   { font-size: 33px; }
    .tg-hero__subheading{ font-size: 12px; margin-bottom: 20px; }
    .tg-hero__arrow     { display: none; }

    .tg-category        { height: 370px; }
    .tg-category__title { font-size: 48px; }
    .tg-category__content { padding: 0 10px; top: 16%; }

    .tg-collection      { padding: 56px 20px; }
    .tg-section-header__title { font-size: 36px; }
    .tg-product-card    { flex: 0 0 calc(100% - 24px); }
    .tg-product-card__title { font-size: 16px; }

    .tg-process         { padding: 80px 28px; }
    .tg-process__cta-heading { font-size: 30px; }
    .tg-process__quote  { font-size: 22px; }

    .tg-artist__content { padding: 48px 28px; }
    .tg-artist__heading { font-size: 32px; }

    .tg-trust           { padding: 56px 24px; }
    .tg-trust__heading  { font-size: 30px; margin-bottom: 44px; }
    .tg-trust__item-title { font-size: 18px; }

    .tg-instagram       { padding: 56px 24px; }
    .tg-instagram__heading { font-size: 34px; }
  }
</style>

<div class="tg-homepage">

  <!-- ============================================================
       1. HERO SLIDER
       ============================================================ -->
  <section class="tg-hero" id="tg-hero" aria-label="Featured hero">

    <!-- Slide 1 -->
    <div
      class="tg-hero__slide is-active"
      style="background-image: url('https://tatianaglen.com/wp-content/uploads/2026/03/Placement-Variety-Living-Area--scaled.jpg');"
      role="group"
      aria-label="Slide 1 of 2"
    >
      <div class="tg-hero__overlay"></div>
      <div class="tg-hero__content">
        <h1 class="tg-hero__heading">Where Art Takes Form</h1>
        <p class="tg-hero__subheading">Handcrafted resin mirrors — sculptural statement pieces for modern interiors.</p>
        <a href="/collections/all" class="tg-hero__cta">View The Collection</a>
      </div>
    </div>

    <!-- Slide 2 -->
    <div
      class="tg-hero__slide"
      style="background-image: url('https://tatianaglen.com/wp-content/uploads/2026/02/Image-4-scaled.jpg');"
      role="group"
      aria-label="Slide 2 of 2"
    >
      <div class="tg-hero__overlay"></div>
      <div class="tg-hero__content">
        <h2 class="tg-hero__heading">Every Piece Is Unique</h2>
        <p class="tg-hero__subheading">No two pieces resolve in the same way. This is how our work comes to life.</p>
        <a href="/collections/all" class="tg-hero__cta">View The Collection</a>
      </div>
    </div>

    <!-- Arrow: Previous -->
    <button class="tg-hero__arrow tg-hero__arrow--prev" id="tg-hero-prev" aria-label="Previous slide">
      <svg viewBox="0 0 18 18"><polyline points="11 4 6 9 11 14"/></svg>
    </button>

    <!-- Arrow: Next -->
    <button class="tg-hero__arrow tg-hero__arrow--next" id="tg-hero-next" aria-label="Next slide">
      <svg viewBox="0 0 18 18"><polyline points="7 4 12 9 7 14"/></svg>
    </button>

    <!-- Dots -->
    <div class="tg-hero__dots" role="tablist" aria-label="Slide navigation">
      <button class="tg-hero__dot is-active" data-dot="0" role="tab" aria-selected="true"  aria-label="Go to slide 1"></button>
      <button class="tg-hero__dot"           data-dot="1" role="tab" aria-selected="false" aria-label="Go to slide 2"></button>
    </div>

  </section>

  <!-- ============================================================
       2. CATEGORY BLOCKS
       ============================================================ -->
  <div class="tg-categories-wrapper">
  <section class="tg-categories" aria-label="Product categories">

    <a href="/collections/mirrors" class="tg-category">
      <div
        class="tg-category__bg"
        style="background-image: url('https://cdn.shopify.com/s/files/1/0706/3525/8054/files/Mirros.jpg?v=1774990393');"
        role="img"
        aria-label="Mirrors collection"
      ></div>
      <div class="tg-category__overlay"></div>
      <div class="tg-category__content">
        <h2 class="tg-category__title">Mirrors</h2>
        <span class="tg-category__link">View More</span>
      </div>
    </a>

    <a href="/collections/sculptures-statues" class="tg-category">
      <div
        class="tg-category__bg"
        style="background-image: url('https://cdn.shopify.com/s/files/1/0706/3525/8054/files/sculptures.png?v=1774990394');"
        role="img"
        aria-label="Sculptures and Statues collection"
      ></div>
      <div class="tg-category__overlay"></div>
      <div class="tg-category__content">
        <h2 class="tg-category__title">Sculptures & Statues</h2>
        <span class="tg-category__link">View More</span>
      </div>
    </a>

  </section>
  </div>

  <!-- ============================================================
       3. THE COLLECTION
       ============================================================ -->
  {%- liquid
    assign collection = collections[section.settings.collection]
    if collection == blank
      assign collection = collections.all
    endif
    assign product_limit = section.settings.product_limit | default: 9
    assign collection_url = collection.url | default: '/collections/all'
  -%}

  <section class="tg-collection">

    <div class="tg-section-header tg-animate">
      <h2 class="tg-section-header__title">{{ section.settings.heading | default: 'The Collection' }}</h2>
      <p class="tg-section-header__desc">{{ section.settings.description | default: 'Handcrafted resin mirrors and sculptural décor objects, designed and made by hand in our Miami & NY studio.' }}</p>
    </div>

    {%- if collection.products_count > 0 -%}
      <div class="tg-product-slider-wrap">
        <div class="tg-product-grid" id="tg-product-slider" data-per-page="3">

          {%- for product in collection.products limit: product_limit -%}
            {%- liquid
              assign featured_image = product.featured_image
              assign product_url    = product.url
              assign product_title  = product.title
              assign first_variant  = product.selected_or_first_available_variant
            -%}

            <article class="tg-product-card" data-product-id="{{ product.id }}">

              {{- '<!-- card link covers entire card -->' }}
              <a href="{{ product_url }}" class="tg-product-card__link" aria-label="{{ product_title | escape }}" tabindex="0">
                <div class="tg-product-card__img-wrap">
                  {%- if featured_image -%}
                    <img
                      src="{{ featured_image | image_url: width: 600 }}"
                      alt="{{ featured_image.alt | escape | default: product_title }}"
                      class="tg-product-card__img"
                      loading="lazy"
                      width="{{ featured_image.width }}"
                      height="{{ featured_image.height }}"
                    >
                  {%- else -%}
                    {{ 'product-1' | placeholder_svg_tag: 'tg-product-card__img tg-placeholder' }}
                  {%- endif -%}

                  <!-- Icon buttons inside image wrap — overlay on hover -->
                  <div class="tg-product-card__actions" onclick="event.preventDefault()">
                    {%- if first_variant.available -%}
                      <a
                        href="{{ product_url }}"
                        class="tg-product-card__icon-btn"
                        aria-label="Add to cart — {{ product_title | escape }}"
                        data-variant-id="{{ first_variant.id }}"
                      >
                        <svg viewBox="0 0 24 24"><path d="M6 2L3 6v14a2 2 0 002 2h14a2 2 0 002-2V6l-3-4z"/><line x1="3" y1="6" x2="21" y2="6"/><path d="M16 10a4 4 0 01-8 0"/></svg>
                      </a>
                    {%- else -%}
                      <span class="tg-product-card__icon-btn tg-product-card__icon-btn--disabled" aria-label="Sold out">
                        <svg viewBox="0 0 24 24"><path d="M6 2L3 6v14a2 2 0 002 2h14a2 2 0 002-2V6l-3-4z"/><line x1="3" y1="6" x2="21" y2="6"/><path d="M16 10a4 4 0 01-8 0"/></svg>
                      </span>
                    {%- endif -%}

                    <button class="tg-product-card__icon-btn" type="button" aria-label="Add to wishlist — {{ product_title | escape }}" onclick="event.stopPropagation()">
                      <svg viewBox="0 0 24 24"><path d="M20.84 4.61a5.5 5.5 0 00-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 00-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 000-7.78z"/></svg>
                    </button>

                    <a href="{{ product_url }}" class="tg-product-card__icon-btn" aria-label="View {{ product_title | escape }}">
                      <svg viewBox="0 0 24 24"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>
                    </a>
                  </div>
                </div>

                <div class="tg-product-card__info">
                  <h3 class="tg-product-card__title">{{ product_title }}</h3>
                  <p class="tg-product-card__price">
                    {%- if product.price_varies -%}
                      {{ product.price_min | money }} – {{ product.price_max | money }}
                    {%- else -%}
                      {{ product.price | money }}
                    {%- endif -%}
                    {%- if first_variant.available == false -%}
                      <span class="tg-product-card__sold-out"> — Sold Out</span>
                    {%- endif -%}
                  </p>
                </div>
              </a>

            </article>

          {%- endfor -%}

        </div>

        <!-- Slider dots (generated by JS) -->
        <div class="tg-product-dots" id="tg-product-dots"></div>
      </div>

    {%- else -%}
      <p style="text-align:center;color:var(--tg-text);padding:40px 0;">No products found in this collection.</p>
    {%- endif -%}

    <div class="tg-collection-footer tg-animate">
      <a href="{{ collection_url }}" class="tg-link-underline">VIEW MORE</a>
    </div>

  </section>

  <!-- ============================================================
       3b. QUOTE BANNER
       ============================================================ -->
  <div class="tg-quote-banner">
    <div class="tg-quote-banner__overlay"></div>
    <p class="tg-quote-banner__text tg-animate">
      Every piece that bears our name carries a shared history and philosophy.
      Shaped by <em>our hands, our home, and our hearts.</em>
    </p>
  </div>

  <!-- ============================================================
       3c. SAGE BAND
       ============================================================ -->
  <div class="tg-sage-band">
    <p class="tg-sage-band__text tg-animate">
      Every piece begins with a story of material, movement, and time.
    </p>
  </div>

  <!-- ============================================================
       4. PROCESS SECTION
       ============================================================ -->
  <section class="tg-process" aria-label="Our process">
    <div class="tg-process__bg"></div>
    <div class="tg-process__overlay"></div>
    <div class="tg-process__content">
      <h2 class="tg-process__cta-heading tg-animate tg-animate--delay-2">
        What Begins as Liquid Slowly Takes Form.
      </h2>
      <p class="tg-process__desc tg-animate tg-animate--delay-3">
        Guided by material, movement, and time, each piece is shaped by hand and finished with intention.
        Resin is poured in layers, allowed to move, settle, and respond to its environment.
        Subtle variations remain — not as imperfections, but as records of the moments and conditions
        that shape the work. No two pieces resolve in the same way. This is how our work comes to life.
      </p>
      <a href="/pages/our-process" class="tg-btn-outline tg-animate tg-animate--delay-4">
        Learn More About the Process
      </a>
    </div>
  </section>

  <!-- ============================================================
       5. MEET THE ARTIST
       ============================================================ -->
  <section class="tg-artist" aria-label="Meet the artist">

    <div class="tg-artist__img-side">
      <img
        src="https://tatianaglen.com/wp-content/uploads/2026/02/WhatsApp-Image-2026-02-10-at-11.31.12-1024x581.jpeg"
        alt="Tatiana and Glen in their Miami studio"
        class="tg-artist__img"
        loading="lazy"
        width="1024" height="581"
      >
    </div>

    <div class="tg-artist__content">
      <span class="tg-artist__label tg-animate">Meet The Artist</span>
      <h2 class="tg-artist__heading tg-animate tg-animate--delay-1">A Family Studio Rooted in Craft.</h2>
      <p class="tg-artist__bio tg-animate tg-animate--delay-2">
        Tatiana & Glen is a family-led studio founded by partners Tatiana and Glen. Each mirror is made by
        Tatiana's hands — informed by more than a decade of experience shaping high-end residences across
        the Americas and Europe, and grounded in a deep understanding of how space, light, and form
        influence the way we live.
      </p>
      <p class="tg-artist__bio tg-animate tg-animate--delay-3">
        Glen's perspective, shaped at the intersection of the mystical and the practical, informs the
        philosophy that surrounds the work. From their home and studio in Miami, the practice creates
        sculptural mirrors rooted in intention, time, and human connection.
      </p>
      <a href="/pages/about" class="tg-artist__link tg-animate tg-animate--delay-4">OUR STORY →</a>
    </div>

  </section>

  <!-- ============================================================
       6. CRAFT & COMMITMENT
       ============================================================ -->
  <section class="tg-trust" aria-label="Our commitments">

    <h2 class="tg-trust__heading tg-animate">Craft & Commitment</h2>

    <div class="tg-trust__grid">

      <!-- Badge 1 — Authenticity -->
      <div class="tg-trust__item tg-animate tg-animate--delay-1">
        <div class="tg-trust__icon" aria-hidden="true">
          <svg viewBox="0 0 52 52" xmlns="http://www.w3.org/2000/svg">
            <path d="M26 4L31 16L44 18L35 27L37.5 40L26 33.5L14.5 40L17 27L8 18L21 16L26 4Z"/>
            <line x1="18" y1="46" x2="34" y2="46"/>
            <line x1="26" y1="40" x2="26" y2="46"/>
          </svg>
        </div>
        <div>
          <h3 class="tg-trust__item-title">Signed & Sealed Authenticity</h3>
          <p class="tg-trust__item-desc">Each piece is accompanied by a certificate of authenticity.</p>
        </div>
      </div>

      <!-- Badge 2 — Warranty -->
      <div class="tg-trust__item tg-animate tg-animate--delay-2">
        <div class="tg-trust__icon" aria-hidden="true">
          <svg viewBox="0 0 52 52" xmlns="http://www.w3.org/2000/svg">
            <path d="M26 6C26 6 10 13 10 26C10 37 17 44 26 48C35 44 42 37 42 26C42 13 26 6 26 6Z"/>
            <polyline points="18 26 23 31 34 20"/>
          </svg>
        </div>
        <div>
          <h3 class="tg-trust__item-title">Two-Year Finish Commitment</h3>
          <p class="tg-trust__item-desc">We stand behind the finish and clarity of every mirror for two years. Should natural wear occur, we offer complimentary refinishing or replacement.</p>
        </div>
      </div>

      <!-- Badge 3 — Delivery -->
      <div class="tg-trust__item tg-animate tg-animate--delay-3">
        <div class="tg-trust__icon" aria-hidden="true">
          <svg viewBox="0 0 52 52" xmlns="http://www.w3.org/2000/svg">
            <rect x="6" y="20" width="40" height="28" rx="2"/>
            <path d="M17 20V15C17 10 20.5 7 26 7C31.5 7 35 10 35 15V20"/>
            <circle cx="26" cy="33" r="3.5" fill="currentColor" stroke="none"/>
            <line x1="26" y1="36.5" x2="26" y2="42"/>
          </svg>
        </div>
        <div>
          <h3 class="tg-trust__item-title">Handled & Insured Delivery</h3>
          <p class="tg-trust__item-desc">Each piece is carefully handled and fully insured from our studio to your door.</p>
        </div>
      </div>

    </div>
  </section>

  <!-- ============================================================
       7. INSTAGRAM / SOCIAL
       ============================================================ -->
  <section class="tg-instagram" aria-label="Follow us on Instagram">

    <h2 class="tg-instagram__heading tg-animate">Our Instagram</h2>
    <p class="tg-instagram__subtext tg-animate tg-animate--delay-1">Follow our store on Instagram</p>

    <div class="tg-instagram__social tg-animate tg-animate--delay-2">

      <!-- Instagram follow button -->
      <a
        href="https://www.instagram.com/tatianaglencollections"
        target="_blank"
        rel="noopener noreferrer"
        class="tg-instagram__follow"
      >
        <svg viewBox="0 0 24 24" aria-hidden="true">
          <path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zm0-2.163c-3.259 0-3.667.014-4.947.072-4.358.2-6.78 2.618-6.98 6.98-.059 1.281-.073 1.689-.073 4.948 0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98 1.281.058 1.689.072 4.948.072 3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98-1.281-.059-1.69-.073-4.949-.073zm0 5.838c-3.403 0-6.162 2.759-6.162 6.162s2.759 6.163 6.162 6.163 6.162-2.759 6.162-6.163c0-3.403-2.759-6.162-6.162-6.162zm0 10.162c-2.209 0-4-1.79-4-4 0-2.209 1.791-4 4-4s4 1.791 4 4c0 2.21-1.791 4-4 4zm6.406-11.845c-.796 0-1.441.645-1.441 1.44s.645 1.44 1.441 1.44c.795 0 1.439-.645 1.439-1.44s-.644-1.44-1.439-1.44z"/>
        </svg>
        FOLLOW US
      </a>

    </div>
  </section>

</div><!-- /.tg-homepage -->

<script>
(function () {
  'use strict';

  /* ============================================================
     HERO SLIDER
     ============================================================ */
  var hero   = document.getElementById('tg-hero');
  if (!hero) return;

  var slides  = hero.querySelectorAll('.tg-hero__slide');
  var dots    = hero.querySelectorAll('.tg-hero__dot');
  var btnPrev = document.getElementById('tg-hero-prev');
  var btnNext = document.getElementById('tg-hero-next');
  var current = 0;
  var total   = slides.length;
  var timer;

  function activate(index) {
    slides[current].classList.remove('is-active');
    dots[current].classList.remove('is-active');
    dots[current].setAttribute('aria-selected', 'false');

    current = (index + total) % total;

    slides[current].classList.add('is-active');
    dots[current].classList.add('is-active');
    dots[current].setAttribute('aria-selected', 'true');
  }

  function startTimer() {
    timer = setInterval(function () { activate(current + 1); }, 4000);
  }

  function stopTimer() { clearInterval(timer); }

  dots.forEach(function (dot, i) {
    dot.addEventListener('click', function () {
      stopTimer(); activate(i); startTimer();
    });
  });

  if (btnPrev) btnPrev.addEventListener('click', function () {
    stopTimer(); activate(current - 1); startTimer();
  });

  if (btnNext) btnNext.addEventListener('click', function () {
    stopTimer(); activate(current + 1); startTimer();
  });

  /* Pause on hover */
  hero.addEventListener('mouseenter', stopTimer);
  hero.addEventListener('mouseleave', startTimer);

  /* Swipe support */
  var touchStartX = 0;
  hero.addEventListener('touchstart', function (e) {
    touchStartX = e.touches[0].clientX;
  }, { passive: true });
  hero.addEventListener('touchend', function (e) {
    var dx = e.changedTouches[0].clientX - touchStartX;
    if (Math.abs(dx) > 40) {
      stopTimer();
      activate(dx < 0 ? current + 1 : current - 1);
      startTimer();
    }
  }, { passive: true });

  startTimer();

  /* ============================================================
     SCROLL-IN ANIMATIONS (IntersectionObserver)
     ============================================================ */
  var animateEls = document.querySelectorAll('.tg-animate');
  if (!animateEls.length) return;

  if ('IntersectionObserver' in window) {
    var observer = new IntersectionObserver(function (entries) {
      entries.forEach(function (entry) {
        if (entry.isIntersecting) {
          entry.target.classList.add('is-visible');
          observer.unobserve(entry.target);
        }
      });
    }, { threshold: 0.1, rootMargin: '0px 0px -48px 0px' });

    animateEls.forEach(function (el) { observer.observe(el); });
  } else {
    /* Fallback: show all immediately */
    animateEls.forEach(function (el) { el.classList.add('is-visible'); });
  }

  /* ============================================================
     PRODUCT SLIDER
     ============================================================ */
  (function () {
    var wrap     = document.querySelector('.tg-product-slider-wrap');
    var slider   = document.getElementById('tg-product-slider');
    var dotsWrap = document.getElementById('tg-product-dots');
    if (!slider || !dotsWrap || !wrap) return;

    var cards    = Array.prototype.slice.call(slider.querySelectorAll('.tg-product-card'));
    var total    = cards.length;
    var current  = 0;
    var gap      = 32;

    /* Responsive: how many cards per page */
    function perPage() {
      return wrap.offsetWidth < 600 ? 1 : wrap.offsetWidth < 900 ? 2 : 3;
    }

    function pages() {
      return Math.ceil(total / perPage());
    }

    /* Offset to card at index i — avoids empty last page */
    function offsetFor(page) {
      var pp     = perPage();
      var idx    = page * pp;
      /* Don't scroll past the last card */
      var maxIdx = Math.max(0, total - pp);
      idx = Math.min(idx, maxIdx);
      var cw = cards[0].offsetWidth + gap;
      return idx * cw;
    }

    function goTo(page) {
      current = Math.max(0, Math.min(page, pages() - 1));
      slider.style.transition = 'transform 0.5s ease';
      slider.style.transform  = 'translateX(-' + offsetFor(current) + 'px)';
      dotsWrap.querySelectorAll('.tg-product-dot').forEach(function (d, i) {
        d.classList.toggle('is-active', i === current);
      });
    }

    function buildDots() {
      dotsWrap.innerHTML = '';
      var p = pages();
      if (p <= 1) { dotsWrap.style.display = 'none'; return; }
      dotsWrap.style.display = '';
      for (var i = 0; i < p; i++) {
        var dot = document.createElement('button');
        dot.className = 'tg-product-dot' + (i === 0 ? ' is-active' : '');
        dot.setAttribute('aria-label', 'Go to page ' + (i + 1));
        dot.dataset.page = i;
        dot.addEventListener('click', function () { goTo(parseInt(this.dataset.page)); });
        dotsWrap.appendChild(dot);
      }
    }

    buildDots();

    /* Rebuild on resize */
    window.addEventListener('resize', function () {
      slider.style.transition = 'none';
      slider.style.transform  = 'translateX(0)';
      current = 0;
      buildDots();
    }, { passive: true });

    /* Touch swipe */
    var touchX = 0;
    wrap.addEventListener('touchstart', function (e) { touchX = e.touches[0].clientX; }, { passive: true });
    wrap.addEventListener('touchend', function (e) {
      var dx = e.changedTouches[0].clientX - touchX;
      if (Math.abs(dx) > 40) goTo(dx < 0 ? current + 1 : current - 1);
    }, { passive: true });
  })();

  /* ============================================================
     PARALLAX — Process section background
     ============================================================ */
  var processBg = document.querySelector('.tg-process__bg');
  if (processBg && window.matchMedia('(min-width: 990px)').matches) {
    window.addEventListener('scroll', function () {
      var section = processBg.parentElement;
      var rect    = section.getBoundingClientRect();
      var visible = rect.top < window.innerHeight && rect.bottom > 0;
      if (visible) {
        var ratio  = (window.innerHeight - rect.top) / (window.innerHeight + rect.height);
        var offset = Math.round((ratio - 0.5) * 80);
        processBg.style.transform = 'translateY(' + offset + 'px)';
      }
    }, { passive: true });
  }

})();
</script>

{% schema %}
{
  "name": "Homepage",
  "tag": "div",
  "class": "homepage-section",
  "settings": [
    {
      "type": "header",
      "content": "Collection Section"
    },
    {
      "type": "collection",
      "id": "collection",
      "label": "Collection to display"
    },
    {
      "type": "range",
      "id": "product_limit",
      "label": "Number of products",
      "min": 3,
      "max": 12,
      "step": 1,
      "default": 9
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Section heading",
      "default": "The Collection"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Section description",
      "default": "Handcrafted resin mirrors and sculptural décor objects, designed and made by hand in our Miami & NY studio. Each piece is created using layered resin techniques, resulting in natural variation in depth, color, and form."
    }
  ],
  "blocks": [],
  "presets": [
    {
      "name": "Homepage"
    }
  ]
}
{% endschema %}
